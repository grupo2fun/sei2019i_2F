<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ahorcado1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Presentation.adminActivity"></activity>
        <activity android:name=".Presentation.categoryActivity" />
        <activity android:name=".Presentation.beginigActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Presentation.roundActivity" />
        <activity android:name=".Presentation.registerUserActivity" />
        <activity android:name=".Presentation.mainMenuActivity" />
        <activity android:name=".Presentation.loginUserActivity">

        </activity>
    </application>

</manifest>